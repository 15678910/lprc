# 인수인계 — lprc

갱신 2026-09-08 밤. 이 파일만 읽고 이어갈 수 있게 적는다.
**전부 배포까지 끝났다.** 작업 트리에는 미추적 PDF 10개만 남아 있다(법령·매뉴얼·자료집·조례·대안 원문 — 전부 **커밋 금지**).
law_watch.py·region_income.py 는 2026-09-06 오후에 푸시돼 그날 아침 예약 실행에는 없었다 — **Actions 첫 실행은 09-07 10:00**. labor.html 상단 점검 줄 날짜로 확인.

## 이번에 끝낸 것

| 커밋 | 무엇 |
|---|---|
| `9c53de9` | 화면 출처 줄에서 개발자용 메모 '(키 불필요)' 제거 |
| `964cb55` | **⑩ 공공부문** — 알리오 수집기·데이터, 워크플로, 문서 |
| `0a7968d` | ⑩ 카드 · **④ 회사 검색** · 입력 커서 유지 |
| `1a0aa78` | **⑦ GDP·GNI·NNI 기준별 격차** |
| `b1ff0a8` `9fcfa5f` `7630457` | 검색칸 한글 IME 버그 세 번에 걸쳐 수정 |
| `0c791d8` | guide '왜 만들었나' — 도구의 목적·철학 6문단 |
| `e044c61` | ⑦ 표에 빼는 기준(실질임금) 추가, GNI·NNI 풀네임 병기 |
| `2e44385` | guide 질문 표 세 줄 → 다섯 줄(⑦⑩ 추가) |
| `c7be20f` | **⑤ 협력사도 검색으로**(④ 와 한 벌로 통합) · guide 표 일곱 줄(⑧⑨ 추가) |
| `9c38419` | **⑧ 평균 가구원 수** — KOSIS 오류 20 해결 |
| `f8b3254` | 회사 재무 주 1회 분리 · 공공부문 조기 포기 |
| `83a300a` | **⑪ 공무원** — 인사혁신처 봉급표 + 지표누리 민간 대비 보수수준 |
| `34e920c` | ⑪ 정액 수당 동결 — 국가법령정보센터 + CPI |
| `7114dc5` `6ffc463` `b1eb7ec` | ⑪ OECD 국제 비교 · 집계행 수정 · '노동자 보수' 로 용어 통일 |
| `730f92e` | 수당 동결 문장을 '물가를 따라갔다면' 방향으로 · 인수인계에 실수 규칙 |
| `e90208c` `9348d85` | ⑩ '같은 5년, 누가 얼마나 올랐나' 비교 + 중위보수 설명 |
| `8827758` | guide '해외에서는 어떻게 정하나' — 민간 6모델·공무원 5모델 (2026-08 수치 검증) |
| `a433b9f` | **⚖️ 원청교섭 가이드 (labor.html)** — 개정 노조법 요건·자가진단·일정 계산기·대응 경로·시행 6개월 수치·판례. 4개 페이지 nav 에 추가 |
| `bad1ed3` | labor 에 '다툼의 근원은 어디에 있나' 구조 진단 절(지불능력·교섭의무 분리 / 열린 기준 / 지연 비대칭) — 사용자 승인 받은 가치판단 포함 |
| (아래) | **이름 변경 + 법령 개정 자동 감시** — 상세는 다음 절 |
| `ac185fe` | 위 항목의 실제 커밋(제목·측정기·law_watch) |
| `59e8be0` | **⑫ 우리 지역** — KOSIS 지역소득 시도별 생산·소득·역외 순수취·노동자 보수 몫. 상세는 아래 절 |
| (아래) | **문서 페이지 공용 사이드바** — `docs/sidenav.js` 하나가 guide·labor·future·about 의 `.card > h2` 를 읽어 좌측 '화면 목차'를 만든다(index 는 자기 sideNav 유지). CSS 는 index 의 .sidenav 와 같은 값 — 한쪽을 고치면 다른 쪽도. 1240px 이상에서만 보인다. file:// 스냅샷에서는 외부 스크립트가 안 실려 확인은 로컬 서버로 |
| `8002537` | index 본문 내부 링크 색 규칙(⑫의 labor 링크가 보라색으로 묻히던 것) |
| `99be3dc` | ⑫ '지금은 얼마를 받나' — 사업체노동력조사 시도/규모별 월급여액(2025)·규모 격차·근로시간 |
| `b414c93` | 문서 페이지 4곳 공용 사이드바 `docs/sidenav.js` |
| `2a8173a` | labor 한계 — 근로자 정의가 빠진 이유(확인된 사실만)·EU 고용추정 대비 |
| `2bc03d8` | 인수인계 — 2023 환노위 대안 원문·교원노조법 확인 결과 |
| `dc58d1e` | **루트를 홈으로** — index.html=홈, measure.html=측정기(상세는 '홈/측정기 분리' 절) |
| `c40f78d` `3872193` | nav — 첫 항목 '🏠 모두를 위한 단체교섭', '소개' 를 측정기 앞으로 |
| `82978cc` `d42493f` | 문구 — '임금 교섭'→'단체교섭', '임금단협'→'단체협약' (사이트·README 전체, 잔여 0 확인). **새 문구에 '임금 교섭'·'임금단협' 쓰지 말 것** |
| `8771bf3` | 홈 '지금 숫자' 에 ①~④ 설명 라벨 |
| (구) | **루트를 홈으로** — `docs/index.html` 은 소개·입구 홈(정적, '지금 숫자' 4칸은 JSON fetch), 측정기 본체는 `docs/measure.html` 로 이동(git mv). 상세는 아래 절 |
| `dcb0bec` | labor 지역 경로에 **학교운영위원회** 절(학부모·지역위원 자격·심의사항·노동 의제 접점) |
| `3417bf6` | **⑬ 회사가 공시에 없을 때** — 국민연금 가입 사업장 내역(30인 이상 5.3만곳, `pension_sites.py`, docs/np/) |
| `135f612` | **업종 평균 기준선** — 한국은행 기업경영분석(KOSIS, `industry_finance.py`) → ④ 직접 입력 빈칸 추정, ⑬ 매출 역산 |
| (미커밋, 별도 폴더) | **조합원 전용 시작 키트** `../lprc-members/` — README(Cloudflare Access 절차)·워크시트·merge.py |
| `b3eccd9` | **지역 경로** — labor 에 '지역 경로' 카드(지역 경제 논리·제도 경로 4종·주민자치회 참고조례 제약), guide 해외 사례에 BCG·노동회의소. 루트의 `26년 주민자치회 참고조례 개정안.pdf`(행안부, 커밋 금지)와 자료집 [메인1] 이승철 토론문이 출처. 지역 단위 데이터는 측정기에 없음 — 화면에 그렇게 적었고, 승수 같은 숫자는 일부러 안 넣었다 |

### 조합원 전용 공간 — 별도 폴더 `../lprc-members` (2026-09-08)

사용자 결정: **측정기·가이드는 공개, 전략(요구안·수용선·실태 원자료)만 잠근다.** 근거는 대화에 있다 —
사실 공유는 정보를 덜 가진 쪽(노조)에 유리하고, 전략 공유는 힘이 약한 쪽에 불리하다.

- 시작 키트는 **공개 저장소 밖** `C:/Users/lacoi/Desktop/lprc-members/` 에 있다(README·index.html·merge.py). git 저장소 아님.
  이 폴더를 lprc 에 넣으면 안 된다.
- 구조: 서버 없음. 워크시트(요구안/수용선/실태)는 localStorage + JSON 내보내기. 담당자가 `merge.py` 로 파일 취합(중위·평균·고용형태별).
  접근 제한은 Cloudflare Pages + Zero Trust Access(이메일 OTP, 50명 무료) — 절차는 그 README. 도메인·비공개 저장소는 사용자가 만든다.
- 로컬 확인: `.claude/launch.json` 의 `members`(8742, `../lprc-members` 서빙). 워크시트 3장·저장·수용선 판독 문장 검증 완료.
- 미해결: 비공시 중소기업의 지불능력 대체 지표 — 국민연금 '가입 사업장 내역' API(공공데이터포털 3046071: 가입자수·당월고지금액,
  상한 6,370,000원 절단)와 한국은행 기업경영분석(업종·규모별)을 ④ 의 대체 경로로 붙일 수 있다. 키 필요(공공데이터포털). 사용자 결정 대기.

### ⑬ 회사가 공시에 없을 때 — pension_sites.py (2026-09-08)

'비공시 중소기업의 지불능력 대체 지표' 결정 → **국민연금 가입 사업장 내역**으로 구현. 국민연금 API
(NpsBplcInfoInqireService)는 2026-09 현재 모든 주소 변형에서 '서비스 없음(코드 12)' — **폐기된 것으로 보고 파일로 간다.**

- 파일: 국민연금공단 모바일 사이트 '공공데이터 개방' 게시판(`getOHAB0019M0List.do`)에서 제목
  `국민연금 가입 사업장 내역_YYYYMMDD` 글의 첨부 `FLxxxx` 를 `fileDown.do?atchFileId=&atchFileSn=1` 로 받는다.
  검색 파라미터는 무시되므로 1~7쪽을 훑는다. **최신은 20251024(자료 2025-09)** — 공단이 매달 올리지 않는다.
  ZIP 36MB → CSV 115MB(cp949, 59만 사업장). 같은 file_date 면 아무것도 안 한다.
- 출력 `docs/np/`: index.json(시도 목록·전국 십분위·한계), `<시도코드>.json`(가입자 ≥30 사업장, 배열의 배열), industry.json(업종명별 n·중위·십분위).
  30인 기준 = 근참법 노사협의회 의무 기준. 52,722곳, 6.6MB(서울·경기 1.8MB 씩 — 시도 고른 뒤 지연 로딩).
- 평균 신고소득 = 당월고지금액 ÷ 가입자수 ÷ 0.09. **상한 637만원(2025.7~2026.6)에서 잘린다** — 화면은 97% 이상이면 '상한' 경고.
  기준소득월액은 전년도 소득 기준·비과세 제외라 실제 임금보다 낮게 치우친다(화면 '읽는 법'에 적음).
- 화면: 기존 검색 부품(`qCfg/coPicker/coResults/coSearch`)에 종류 `'np'` 를 추가했다(QID `npq`). **한글 IME 처리를 그대로 물려받는다** —
  새 검색칸을 따로 만들지 말 것. `coSearch(q,ex,k)` 가 k==='np' 면 `npIndex()`(현재 시도 샤드)를 뒤진다. 선택은 `S.np.sel`(코드 `시도-행번호`).
- 워크플로: 일요일 회사 재무 잡에 붙였다(주 1회). `GEN` 에 `docs/np` 추가.
- **업종 평균 기준선 — industry_finance.py** (2026-09-08 추가): 한국은행 기업경영분석을 **KOSIS(orgId 301)** 로 받는다 — BOK 키 없이 KOSIS 키로 된다.
  표 `DT_501Y006`(손익 지표, 매출액영업이익률 611) · `DT_501Y009`(생산성, 부가가치율 9064·노동소득분배율 9074) · `DT_501Y086`(분위수, 611·506 × 평균/1분위/중위/3분위).
  파라미터: itmId 는 표마다 하나(예 13103134573999), objL1=업종(129, 전산업은 `ZZZ00`), objL2=규모(A 종합/L/J/M/D/S), objL3=계정, objL4=분위(Y086).
  **인건비÷매출액 = 부가가치율 × 노동소득분배율** 로 계산해 넣었다(원자료에 없는 추정 — 화면에 그렇게 적음). 2024년 기준, 59KB.
  세세 업종(C301·C303 등)은 '종합' 규모만 있다 → 화면 `bokPick()` 이 A 로 물러서고 그 사실을 표시.
  화면: `bokBench(ctx)` 공용 블록을 ④ 직접 입력과 ⑬ 에 붙였다. ④ 는 매출·영업이익이 비면 업종 비율로 **추정해 산식을 세우고 '(업종추정)' 라벨**을 단다. 상태 `S.bok={code,scale}`.
  워크플로 매일 잡(호출 3번). 연 1회(10~11월) 갱신.

### 홈/측정기 분리 (2026-09-08, 사용자 지시 '방법 A')

- **URL 구조**: `/` = 홈(index.html), `/measure.html` = 측정기(옛 index.html 그대로). 다른 네 페이지의 `href="./"`·`./#cN` 은
  전부 `./measure.html`·`./measure.html#cN` 으로 바꿨고, 모든 nav 맨 앞에 `🏠 홈` 을 넣었다. README 구조도 갱신.
- **옛 링크 호환**: 홈의 `<head>` 인라인 스크립트가 `#cN`·`#secN` 해시로 들어오면 `measure.html`+해시로 `location.replace`.
  `hashchange` 에도 반응(같은 문서 안 해시 변경은 스크립트를 다시 안 돌리므로).
- **`#cN` 앵커는 원래 존재하지 않았다** — 카드 id 는 `sideNav()` 가 렌더 뒤 `sec0…` 으로 붙인다. 그래서 labor 의 `./#c5` 는
  줄곧 맨 위로만 가던 죽은 링크였다. 이제 measure 의 `sideNav()` 가 `#cN` 을 N번째 번호 카드로, `#secN` 을 id 로 해석해
  한 번 스크롤한다(`navJumped`). **⑩⑪⑫ 는 지연 로딩이라** 목표가 그쪽이면 `loadPS/PV/RG` 를 즉시 부르고 셋 다 올 때까지
  render 마다 다시 맞춘다 — 그전에 `navJumped` 를 세우면 카드가 자라며 위치가 밀린다(검증에서 −200px 로 확인).
- 검증 함정: 브라우저 도구로 `measure.html#c5` → `#c12` 처럼 **같은 경로에 해시만 다른 navigate 는 재로드가 아니다**.
  새 코드를 시험하려면 다른 페이지를 거치거나 `location.reload()`.
- 홈은 `sidenav.js` 를 싣지 않는다(카드형 홈이라 목차 불필요). 홈의 '지금 숫자' 는 money_macro(물가·갱신일)·law_watch·region 을 읽는다.

### ⑫ 우리 지역 — region_income.py (2026-09-06)

'지역 경제' 논거를 숫자로 뒷받침하는 카드. KOSIS 지역소득 **2020년 기준 계열**을 쓴다.

- 표: `DT_1C96`(1인당 GRDP·지역총소득·가계총처분가능소득, 천원) · `DT_1C95`(지역외순수취본원소득, 백만원) ·
  `DT_1C94`(제도부문별 소득계정 — itmId=T2 원천, objL2=A00 피용자보수 + F00 총본원소득잔액, objL3=00 합계).
  **`DT_1C8x` 는 구계열(2022년까지)** — 헷갈리지 말 것. 2024년까지 나온다.
- 표를 찾는 법: `statisticsData.do?method=getMeta&type=TBL|ITM&tblId=…` 로 이름·항목을 본다(키 필요).
- **부호 규칙**: `net_inflow` 는 통계청 값 그대로 — 양수 = 밖에서 들어온 소득, 음수 = 밖으로 나간 소득.
  화면 문장도 그대로 따른다. 충남·울산 −22%, 서울 +13% 가 나오면 맞는 것.
- 파생값은 화면(`rgDerive`)에서: GRDP = 지역총소득 − 순수취, 순수취/GRDP, 노동자 보수/지역총소득, 가계소득/1인당 GRDP.
- **노동자 보수 몫은 거주자 기준 지역총소득 대비**라 ⑦의 GDP 대비 노동 몫과 숫자를 견주면 안 된다(화면에 적어 둠).
- 로컬 실행은 `budget/.env.local` 의 KOSIS 키를 환경변수로만 넘긴다(값 출력·커밋 금지). Actions 에는 시크릿이 있다.
  `docs/region.json` 은 `docs/*.json` 글롭에 잡혀 자동 커밋된다.
- 자료 한계는 JSON `limits` 에 있고 화면이 그대로 찍는다(시도 단위·통근 혼재·잠정치).
- **임금 블록('지금은 얼마를 받나')**: 고용노동부 `DT_118N_MON060`(orgId 118) 시도/산업/규모별 상용 월급여액·총근로시간.
  `prdSe=M` 으로 불러도 **연간**만 온다(2025 까지). 규모는 전규모·1~29인·300인 이상 셋 — 지역 안 대·소기업 격차용.
  **시군구 임금 평균은 KOSIS 오픈API 에 없다** — 지역별고용조사(D_32)는 취업자 수뿐, 임금수준별은 전국뿐. 시도가 하한.
  2026년 값은 지역소득·임금 둘 다 없다(사용자가 2026 기준을 원했지만 통계가 없다 — 화면에 그 이유를 적었다).
- **시도 이름 매핑 함정**: 지역소득은 '충청남도', 임금표는 '충남'. 앞 두 글자로 자르면 '충청' 이 된다.
  충청·전라·경상은 첫 글자+셋째 글자(`rgWage` 의 `short()`), 나머지는 앞 두 글자.
- **검증 함정**: ⑫ 는 스크롤로 지연 로딩된다. 브라우저 도구로 확인할 때 데이터 도착 전에 DOM 을 읽으면
  '블록 없음' 으로 잘못 판단한다(이번에 30분 허비). 로딩을 폴링으로 기다린 뒤 읽을 것.

### 이름 — '모두를 위한 단체교섭' / '측정기' (2026-09-06, 사용자 지시)

- 사이트 제목은 **'모두를 위한 단체교섭'**, 도구 명칭은 **'측정기'** 다. '계산기' 는 전 페이지에서
  퇴출했다(README 포함). 새 화면·문구를 쓸 때 '계산기' 라고 쓰면 안 된다 — 용어 실수 규칙과 같은 유형.
- 예외 하나: labor 의 날짜 기능은 '일정 자동 계산' 으로 적었다(제품명이 아니라 동작 설명이라
  '일정 측정기' 는 한국어가 어색하다).
- 관계 정의(사용자와 합의): **측정기는 다툴 숫자를 만들고, 원청교섭 가이드는 그 숫자가 오를
  테이블을 여는 경로를 안내한다.** 측정기는 자동 갱신되는 측정 도구, 가이드는 기준일 박힌 지도.

### 법령 개정 자동 감시 — law_watch.py (2026-09-06)

'지도(labor.html)는 사람이 다시 그려야 한다' 의 절반을 자동화했다. **본문 갱신은 여전히 수동**이지만,
본문이 낡았는지는 기계가 안다.

- `law_watch.py` 가 매일 워크플로에서 노조법·시행령·시행규칙의 **현행 공포번호**를 법제처와 대조해
  `docs/law_watch.json` 을 쓴다. labor.html 이 이를 읽어 맨 위에 '개정 없음' 확인 줄 또는
  개정 경고(warn)를 띄운다. 조회 실패 시 기존 파일 보존 후 exit 0 (워크플로 안 죽음).
- **BASELINE 이 이중 장부다**: labor 본문을 새 법령에 맞춰 고치면 law_watch.py 의 BASELINE
  (법 21045 / 영 36159 / 규칙 463, 모두 시행 20260310)도 같이 올려야 경고가 꺼진다.
- 판례·통계·행정해석 변동은 자동 감지 불가 — 화면에도 그렇게 적어 두었다. 억지로 판례 API 를
  긁어 '새 판례 없음' 을 표시하는 건 거짓 안심이라 하지 않았다.
- 로컬 검증용 `.claude/launch.json` 추가(python -m http.server 8741, docs 서빙) —
  file:// 에서는 fetch 가 막혀 law_watch 표시가 안 뜬다. 개정 경고 경로는 mock JSON 으로 검증 완료.

### ⚖️ 원청교섭 가이드 (labor.html) — 2026-09-06

계산기(①~⑪)와 달리 **자동 갱신되지 않는 제도 설명 페이지.** 화면에 기준일(2026-09-06)을 박아 두었다.
갱신할 때는 통계(경향 202608301738001 의 고용노동부 집계, 민주노총 8.21 집계)와 판례(파기환송심 결과,
해석지침 확정본 공표 여부)를 다시 확인해야 한다.

- **법령 원문은 법제처 DRF 로 받았다** (OC=test 열려 있음, HANDOFF 기존 절 참조).
  `lawService.do?OC=test&target=law&MST=<번호>&type=XML` — 노조법 `273667`, 시행령 `283803`, 시행규칙 `284021`.
  `target=eflaw` 는 `efYd=` 없이 부르면 XML 이 아니라 오류 페이지가 온다.
- **개정(제21045호, 2026-03-10 시행)의 실체**: 제2조2호 후단(실질적·구체적 지배·결정 = 사용자),
  제2조5호(쟁의 범위), 제3조(손배 제한), 제2조4호 라목 삭제. **창구 단일화 조문(29조의2~5)은 그대로.**
  하청 국면은 시행령이 처리 — 제14조의11 **③④ 신설**(④: 원청 교섭의 분리 판단은 별도 기준을 우선),
  제14조의3③·제14조의5⑤ 단서(원청 사건 노동위 결정 10일+10일 연장).
- **판례 함정**: 교섭단위 분리 리딩케이스는 **2015두39361**(2018-09-13). 사용자가 준 '2022두44446' 은
  존재를 확인 못 했다(쓰지 말 것). 2026년 대법 사건번호(HD현중 2018다296229 등)는 **언론 보도 기준**이라
  화면에도 그렇게 적었다.
- **루트의 PDF 들은 커밋 금지.** 법령·매뉴얼·자료집·브리핑 PDF 가 미추적으로 남아 있다(공개 저장소 + 저작권).
  노동부 「원·하청 상생 교섭절차 매뉴얼」(2026-02)과 사회주의대회 자료집 세션1(민주노총 집계·동희오토 타임라인)이
  페이지의 출처다. pypdf 로 읽었다(Read 의 PDF 렌더링은 이 환경에서 안 됨).
- 남은 것: 동희오토 행정소송·CJ대한통운 파기환송심 결과 나오면 반영, guide 질문 표에 줄 추가 여부는 미정.
- **근로자 정의가 빠진 경위**: labor '한계' 에 '왜 빠졌나' 를 넣었다. 확인된 것만 썼다 — 제2조1호 미개정(법제처 원문),
  노동계 요구 '근로자 추정' 조항 미반영(매일노동뉴스 2025-04-15), EU 지침 2024/2831 의 고용 추정.
  "2023년 환노위 대안에서 근로자 정의가 삭제됐다" 는 경위는 검색으로 **확인 못 해 화면에 안 넣었다**. 넣으려면 의안정보 원문 확인.
- **학교운영위원회 절**(labor 지역 경로): 초·중등교육법 제31·32조(법제처 DRF MST 286065)·시행령 제58·63조·서울교육청 조례
  제10102호 원문 기준. 핵심 차이 — 자격이 '학부모'(주민등록 불필요), 조례에 정치적 중립·해촉 조항 없음(확인), 지역위원은
  '학교운영에 이바지하고자 하는 자'로 노조 활동가 진입 가능. 조례 PDF 2개가 루트에 추가됨(커밋 금지, 총 12개). 타 시도 조례는 다르다.
- **2023 환노위 대안 원문 확보**: 루트에 `2123038_환경노동위원회_위원회제출안…(대안).pdf`·재의요구서·교원노조법 PDF 가 추가됐다(모두 **커밋 금지**).
  대안 원문으로 확정: 2023 안은 제2조2호 후단·5호('결정' 삭제)·제3조②③뿐, 근로자 정의 개정 없음. labor 본문과 모순 없음.
  교원노조법 제2조3호 단서 '강사 제외'·제8조 쟁의 금지·제3조 대학교원 부분 위헌(2026.7.23) 확인. 노조법 2차 개정 시민 초안 재료는
  `fundamentalrights/.claude/HANDOFF-시민사회기본법.md` 뒷부분에 적었다.
- **범위 밖으로 이관**: 시민사회기본법(가칭)·세금지정제 시민 초안은 사용자 지시로
  `fundamentalrights` 프로젝트로 넘겼다(`fundamentalrights/.claude/HANDOFF-시민사회기본법.md`, 미커밋).
  이 측정기에서는 다루지 않는다.

### 새 카드를 쓰기 전에 — 이번에 반복해서 틀린 것

같은 것을 기존 화면이 뭐라 부르고 어떻게 다루는지 **먼저 확인할 것.**
이번 세션에서 세 번 어겼고 세 번 다 나중에 고쳐야 했다.

- **용어** — ⑪ 에서 D1 을 '피용자보수' 라고 썼는데, 이 프로젝트는 줄곧
  **'노동자 보수'** 로 적어 왔다(⑦ 노동 몫, about, future). 한 화면 안에서 같은 개념이
  두 이름으로 불렸다. → 새 지표를 쓰기 전에 `grep` 으로 기존 표기를 찾아볼 것.
- **부품** — ④ 회사 검색을 만들면서 ⑤ 협력사 선택은 드롭다운으로 두어, 한 화면에
  두 방식이 섞였다. IME 를 세 번 고치는 동안 ⑤ 는 손도 안 댄 상태였다.
  → 같은 성격의 UI 는 **한 벌로 만들 것**(`qCfg`/`coPicker`).
- **표현** — 실질 가치를 "2019년 기준 11.29만원의 값어치" 로 적었더니 읽히지 않았다.
  기준 시점이 문장 안에서 옮겨 다니기 때문이다. **"물가를 따라갔다면 얼마여야 했나"**
  로 방향을 뒤집으니 바로 읽힌다. 교섭에서 쓰는 문장('얼마를 올려야 원래대로냐')과도 맞는다.

### 한글 IME — 다시 건드릴 때 주의

검색칸에서 한글이 자모로 풀리던 문제를 **세 번** 고쳤다. 원인은 하나였다 —
`render()` 가 `innerHTML` 을 통째로 갈면 진행 중인 IME 조합이 깨진다.

- 조합 중(`compositionstart`~`end`)에는 **입력칸을 다시 그리지 않는다.**
- 특히 **음절이 확정되는 순간**이 함정이다. `compositionend` 와 다음
  `compositionstart` 가 붙어서 오는데, 그 사이에 그리면 방금 시작된 조합이 깨진다.
  그래서 검색 중에는 `render()` 를 아예 부르지 않고 결과 목록만 갈아 끼운다.
- `oncompositionstart` / `oncompositionend` 는 **HTML 인라인 속성이 아니다**
  (`onclick`·`oninput` 과 달리 표준 목록에 없어 브라우저가 무시한다). document 위임으로 건다.
- ④ 원청과 ⑤ 협력사가 **같은 칸을 쓴다**(`qCfg`/`coPicker`/`coResults`). 두 벌로 두면
  한쪽만 고치는 일이 생긴다 — IME 를 세 번 고치는 동안 ⑤ 는 손도 안 댄 상태였다.
- 엔터는 `e.isComposing` 으로 걸러야 한다. 조합 확정 엔터와 결과 선택 엔터는 다른 동작이다.
- **검증할 때 `compositionend` 를 맨 끝에 한 번만 보내면 안 된다.** 실제 키보드는
  음절마다 end/start 를 반복한다. 그 경로를 안 밟으면 버그가 통과한다(두 번 놓쳤다).

`render()` 는 입력칸의 포커스와 커서를 잃는다. 다시 그리기 전 위치를 기억했다가
`restoreFocus()` 로 되돌린다(부모에서 몇 번째 자식인가로 자리를 잡는다).

### ⑦ 기준별 격차 — 부호가 뒤집히는 지표는 쓰지 않는다

`money_macro_monitor.py` 의 `_gap_wedges()` 안에 `bases` 로 들어 있다.
OECD `DSD_NAMAIN10@DF_TABLE2` 의 B5G(국민총소득)·B5N(국민순소득), 원화 연쇄물량. 키 불필요.

총액을 1인당으로 바꿀 때 인구 계열이 필요 없다 — 1인당 실질 GDP 에 총액 대비
비율만 곱하면 된다(인구가 같은 분모라 상쇄).

**시작 연도를 바꿔 가며 재는 민감도 계산이 핵심이다.** GNI 는 시작 연도에 따라
부호가 뒤집힌다(−4.3 ~ +0.9%p). 그런 지표는 상대가 다른 해를 들고 오면 그대로
뒤집히므로 교섭 근거로 못 쓴다. NNI 는 모든 시작 연도에서 음수라 방향이 일관된다.
이 카드가 이미 '모든 구간에서 부호가 같다' 를 근거의 기준으로 쓰고 있어 같은 잣대를 적용했다.

감가상각이 진짜 구멍이다 — GDP 기준 8.5%p 격차가 NNI 기준으로는 3.5%p 가 된다.

### 로컬에서 수집기를 돌릴 때 — 반드시 확인할 것

로컬에 **BOK·KOSIS·DART 키가 없다**(Actions Secrets 에만 있다).
`money_macro_monitor.py` 를 로컬에서 돌리면 `inequality.kr`(한국 분배지표)이
`null` 이 된다. 그대로 커밋하면 화면에서 지표가 사라진다.

커밋 전에 반드시 대조할 것:

```bash
git show HEAD:docs/money_macro.json > /tmp/old.json
# 최상위 키별로 크기를 비교해 null 이 되거나 줄어든 곳이 없는지 본다
```

(Git Bash 와 Windows Python 이 `/tmp` 를 다르게 해석한다. 파이썬으로 다룰 파일은
스크래치패드 절대경로를 쓸 것.)

## 공공부문(알리오) 기술 참고

`public_sector.py` → `docs/public_sector.json` + `docs/ps/NN.json` → `index.html` 의 ⑩ 카드.
알리오 355곳, 21개 항목, 6개 연도. 키 불필요. 워크플로에 붙어 매일 돈다.

### 막혔던 CSV 파일명 문제 — 풀 필요가 없었다

이전 세션은 `statisticsDown.json?f=…&s=…` 의 `s=` 규칙을 찾으려 했다. 그 방향은 막다른 길이다.
`일반현황`은 화면에 하드코딩된 특수 케이스라 되는 것이었고, 항목별 파일명은 서버가 쥐고 있다.

**화면이 실제로 쓰는 JSON 이 따로 있다.** `/resources/vue/app.js` 에서 찾았다.

```
GET /statisticsSearch/findSingleItemSearchList.json
    ?pageNo=1&countPerPage=100&reportFormNo=<rf>&itemNo=<ino>
```

- `treeCode` = `"<reportFormNo>-<itemNo>"` (findItemTreeList.json 의 각 잎 노드)
- 응답: `{status:"success", data:{result:[...], page:{totalPage,...}}}`
- 한 행 = 한 기관 × 6개 연도(`yy5`..`yy0`) + `apbaId/apbaNa/apbaTypeNm/jidtDptmNa/itmsUnitNm`
- `countPerPage` 상한은 **100**. 355곳이면 4쪽을 돈다.
- CSV 파싱도, 인코딩 씨름도, 파일명 추측도 없다.

관련 다운로드 엔드포인트(`downExcelSingleItem.json`)도 확인했지만 쓸 이유가 없다.

### 반드시 알고 있어야 할 함정 세 가지

1. **연도 기준이 섞여 있다.** 알리오 각주 그대로: `yy5`..`yy1` = 결산, `yy0`(=당해년도) = **예산편성** 기준.
   실제로 한전은 결산 96,381천원 → 예산 87,389천원으로 뚝 떨어진다. 섞어 평균 내면 안 된다.
   화면의 기본 비교는 결산 마지막 해만 쓴다(`psLast`).
   `GB_alioYear` 는 화면에서 정규식으로 읽는다 — 하드코딩하면 해가 바뀔 때 조용히 틀린다.

2. **단위를 짐작하면 틀린다.** `평균근속연수`는 '년'이 아니라 **월**로 온다(`itmsUnit=5`).
   그래서 수집기는 우리가 적어 둔 단위 대신 **응답의 `itmsUnitNm` 을 따르고**, 다르면 diag 에 WARN 을 남긴다.

3. **중간 노드는 값이 안 온다.** `20202-GI020102`(일반정규직>현원)처럼 자식이 있는 노드는 0건이다.
   잎인 `20202-GI02010201`(현원>계)을 써야 한다.

그리고 **0 이 '없음'인 항목이 있다.** 신입사원 초임 0은 0원이 아니라 그 해 신규채용이 없었다는 뜻이다
(대한석탄공사·한국통계정보원). 중위값과 등수에서 빼고, 근속 모형도 계산하지 않는다.

### 확인된 항목 코드

`I. 기관운영` 아래. 전체 목록은 `findItemTreeList.json`(600개) 참고.

| 쓰는 것 | treeCode |
|---|---|
| 1인당 평균보수(일반정규직 / 남 / 여) | `20601-GI0101` / `GI0111` / `GI0112` |
| 기본급 · 성과상여금 · 경영평가 성과급 | `20601-GI0102` / `GI0106` / `GI0107` |
| 무기계약직 1인당 평균보수 | `20601-GI0301` |
| 신입사원 초임(합계) | `20601-GI0208` |
| 상시종업원수(계 / 남 / 여) | `20601-GI0109` / `GI0113` / `GI0114` |
| 평균근속**월**(계 / 남 / 여) | `20601-GI0110` / `GI0115` / `GI0116` |
| 임직원 총계 | `20202-GI03` |
| 일반정규직·무기계약직 현원(계) | `20202-GI02010201` / `GI02020201` |
| 기간제 계 · 소속외인력 계 | `20202-GI050103` / `GI050304` |
| 여성 현원 합계 | `20202-GI0401` |
| 일반정규직 총신규채용 | `20402-GI010201` |

아직 안 쓴 것 중 쓸 만한 것: `20202-GI050304` 하위의 파견/용역/사내하도급 **내역별**,
`20202-GI050104` 정규직 전환 실적·비율, `20601-GI0117/0118` 평균임금 비율(성별),
`20202-GI06` 자회사, `20402-GI010203~010207` 신규채용 중 여성·장애인·지역인재·고졸.

### 설계 결정 (지킨 것)

- **기능별 11개 분류** — 알리오 공식 분류(공기업/준정부/기타)는 법적 지위라 임금 비교에 못 쓴다.
  기관명 규칙을 먼저 보고, 안 걸리면 주무부처로 넘긴다. 기관명이 부처보다 안정적이다
  (2026년 개편으로 기후에너지환경부·산업통상부·재정경제부·지식재산처 등으로 이름이 다 바뀌었다).
  **연구·학술 규칙이 맨 앞**이다 — 에너지경제연구원의 노동시장은 발전소가 아니라 다른 출연연이다.
  분류 근거를 기관마다 `why` 로 실어 화면에 표시한다. 우리가 만든 분류라고 못 박아 뒀다.
  conects 사이트는 **분류 체계만 참고**했고 데이터 출처로 쓰지 않았다.
- **④ 로직을 쓰지 않았다** — 공공기관은 이익이 없어 지불능력 계산이 성립하지 않는다.
  총인건비 인상률은 기재부 지침 소관이고 공개 API 가 없어 **이용자 직접 입력**이다.
  대신 355곳 평균보수의 전년비 중위값을 같이 보여준다 — 지침 수치가 아니라 **실제 결과**라
  "지침대로 올렸는데 왜 체감이 다른가"를 물을 수 있다.
- **성별 격차를 근속으로 분해한다** — DART 에 없는 축이라 여기서만 가능하다.
  `보수 = 신입 초임 + (근속 1년당 증가분 × 근속)` 직선을 그 기관 공시값만으로 세운다.
  초임을 절편으로 쓰는 게 핵심 — 비례로 두면 근속 0년의 보수가 0원이 되어 격차가 부풀려진다.
  직종 구성이 갈리는 기관(병원 등)엔 안 맞는다고 화면에 크게 적어 뒀다. 결론이 아니라 질문의 출발점이다.
- **실패하면 조용히 넘기고 로그를 남긴다** — 항목별로 독립 실패하고, 전부 실패할 때만 기존 파일을 보존한다.
  원인은 출력 JSON 의 `diag` 배열에 남는다. 트리에서 코드가 사라지면 먼저 걸린다.

### 겸사겸사 고친 것

`data.yml` 의 커밋 단계가 `git add docs/*.json` 만 해서 **`docs/co/` 가 수동 커밋 이후 자동 갱신되지 않고 있었다**
(글롭이 하위 폴더를 안 잡는다). `docs/co docs/ps` 를 디렉터리째 넣도록 고쳤다.
push 실패 시 재시도용 백업/복원도 하위 폴더를 같이 다루게 했다.

## 자동 수집 — 오늘 확인한 것 (2026-08-21)

바뀐 워크플로가 실제로 돌았다. 결과는 이렇다.

- **공공부문은 Actions 에서 정상 수집된다.** 예약 실행에서 21/21 항목·355곳을 4분에
  받았다. 한때 '해외 IP 차단' 으로 판단했으나 **틀렸다** — 예약 실행이 도는 중에
  수동 실행과 진단을 겹쳐 걸어서 나중 것들이 막힌 것이었다.
  **알리오는 같은 출처의 동시 접속을 막는다. 실행을 겹치지 말 것.**
- **`docs/co/` 갱신이 재개됐다.** 커밋 단계 글롭 버그를 고친 결과다.
- `inequality.kr`·평균 가구원 수 모두 Actions 에서 채워진다.

### 주기가 갈렸다

```
매일 KST 10:00    물가·임금·GNI·공공부문        (몇 분)
일요일 KST 11:00   + 회사 재무 (DART 1,883곳)   (3시간+)
수동 실행          companies 입력으로 회사 포함 여부를 고른다
```

회사 재무는 실측 3시간이다. 회사 한 곳당 DART 를 최대 7번 물어야 하고(직원현황·손익을
최근 3개 사업연도까지 + 업종) 1,883곳이면 13,000회다. 그런데 사업보고서는 연 1회
공시다. 매일 돌 이유가 없어 주 1회로 뺐다.

8곳에서 1,883곳으로 늘린 것이 수동 작업이었고 그 규모로 Actions 가 돌아 본 적이
없어 이제야 드러났다. 그전 실행이 5~6분에 끝난 기록은 8곳 시절의 것이다.

### 오래 매달리지 않게 해 둔 것

닿지 못할 때 몇 시간을 헛도는 게 진짜 문제였다. 타임아웃 40초 × 재시도 3회면
한 요청이 126초를 잡고, 84요청이면 3시간이다. 실제로 실행을 두 번 끊어야 했다.

  public_sector.py — 타임아웃 12초 · 재시도 2회 · 트리 조회를 관문으로 삼아
                     못 닿으면 즉시 중단 · 연속 3항목 실패 시 나머지 건너뜀
                     (실측: 2.2초에 끝나고 기존 파일 보존)
  워크플로 — 공공부문 timeout-minutes 20, 회사 재무 200

## 남은 일

**미해결 없음.** ⑧ 평균 가구원 수까지 채워 ①~⑪ 이 모두 값을 갖는다.

**⑪ 수당 — 정액급식비는 해결됐다.** 국가법령정보센터 API(OC=test)로 연혁을 훑어
동결 구간(13만원 10년·14만원 6년)과 실질 부족분을 자동 계산한다. 남은 것은
직급보조비·위험근무수당·특수업무수당 — **별표에 있어 자동화 불가**(첨부 HWP만 옴).
붙이려면 HWP 파싱이 필요한데 표준 라이브러리 원칙과 충돌한다. 수동 입력이 현실적.

**하면 좋은 것**

- ⑩ 을 ⑤⑥ 과 연결 — 소속외인력·무기계약직 인원이 이미 있으므로 격차·해소 경로 계산에
  자동으로 넘길 수 있다. 지금은 이용자가 보고 옮겨 적어야 한다.
- **지방공공기관** — 클린아이(cleaneye.go.kr)에 별도 공시. ⑩ 에 없다. 붙일지는 미정.
- ⑪ 에 경찰·소방·교원·군인 봉급표 붙이기 — `public_servant_pay.json` 에 원본이 이미 있다.
  일반직 외에는 구조화만 안 해 둔 상태다.
- 알리오에서 아직 안 쓴 항목: 소속외인력의 파견/용역/사내하도급 **내역별**,
  정규직 전환 실적·비율, 평균임금 비율(성별), 자회사, 신규채용 중 여성·장애인·지역인재·고졸.

**남은 판단 하나**

- guide '왜 만들었나' 4문단의 "격차는 삶의 질을 떨어뜨린다" 는 **가치 판단**이다.
  도구의 목적 선언으로 두었고 바로 아래 '중립 원칙' 과는 층이 달라 충돌하지 않는다.
  톤을 낮추길 원하면 그 문단만 조정하면 된다.

## ⑪ 공무원 — 다시 건드릴 때

`public_servant.py` → `docs/public_servant.json`(34KB) + `public_servant_pay.json`(169KB).
둘 다 키가 필요 없다.

- **봉급표** — 인사혁신처 `bizSalary/{연도}/`. HTML 표로 그대로 있다. 연도별 URL 규칙이 같다.
  표 구조가 해마다 바뀐다(2023년 이전 20종 → 이후 11종). 그래서 **해석하지 않고**
  머리행·데이터행을 그대로 담고 일반직만 계급×호봉으로 구조화한다.
- **민간 대비 보수수준** — 지표누리 OpenAPI 는 승인 심사를 요구하지만,
  화면이 쓰는 주소는 키가 필요 없다.
  `showStblGams3.do?stts_cd=102101&idx_cd=1021&freq=Y&period=N`

**본봉과 총액을 섞지 말 것.** 봉급표는 본봉뿐이고 알리오 신입 초임은 수당·성과급을 더한
합계다. 그냥 나누면 9급 초임이 공공기관의 64.6% 로 나와 교섭에서 그대로 반박당한다
(수당 90만원을 넣으면 91.8%). 그래서 **수당을 넣기 전에는 비교를 보여주지 않는다.**
접근율도 수당·연금부담금 포함 총보수 기준이라 봉급표 숫자와 곱하거나 빼면 안 된다.

정액 수당(정액급식비)은 국가법령정보센터 API(OC=test, 키 불필요)에서 연혁을 훑어
동결 구간을 만든다. 금액이 조문 본문에 있는 것은 **정액급식비뿐**이고 직급보조비·
위험근무수당·특수업무수당은 별표에 있어 자동화가 안 된다(별표는 첨부파일 링크만 온다).

OECD 국제 비교는 DF_GOV_TRANSACTION 의 D1/GDP 를 쓴다. 직급별 급여 국제 비교는
OECD 가 열어 두지 않았다(DF_GOV_2025 는 404). **응답에 집계행이 섞여 있다** —
OECD·OECD_REP·EUOECD 를 빼지 않으면 국가 수와 순위가 함께 부풀려진다(44개국 42위 →
41개국 39위). 평균은 직접 내지 말고 OECD_REP(OECD average country)를 쓸 것.

공무원노조법 제10조 — 법령·조례·예산으로 정해지는 내용은 단협 효력이 없다.
⑪ 은 교섭 문안이 아니라 공무원보수위원회 → 기재부 예산 라인을 향한 근거다.

## KOSIS — 다시 건드릴 때

오류 20(필수요청변수값 누락)에 오래 막혀 있었다. 원인은 두 가지였다.

- **엔드포인트.** `statisticsData.do` 는 무엇을 넣어도 오류 20 이다.
  화면의 [OpenAPI] 가 만들어 주는 것과 같은 `Param/statisticsParameterData.do` 를 쓰고,
  `itmId` 와 `objL1` 을 반드시 넣는다.
- **통계표를 이름으로 찾은 것.** '가구원수' 로 검색하면 가계동향조사의
  '가구원수별 가구당 월평균 가계수지' 가 먼저 잡힌다. 가구원수를 분류 기준으로 쓸 뿐
  평균 가구원수가 아니다. **통계표를 지정할 것.**

```
101 / DT_1JC1501  인구총조사 가구형태별 가구 및 가구원
objL1=00(전국)  itmId=T1100(총가구)+T1200(총가구원)  prdSe=Y  jsonVD=Y
```

KOSIS 에 '평균 가구원수' 항목은 없다 — 총가구원 ÷ 총가구로 만든다.
`objL1=ALL` 은 시군구까지 와서 4만 셀 제한(오류 31)에 걸린다.

수집만 하고 화면이 안 쓰던 문제도 있었다. `household_size` 가 JSON 에 있어도 어디서도
읽지 않아 보이지 않았다. **수집기를 고쳤으면 화면이 그 값을 쓰는지 반드시 확인할 것.**

## 현재 앱 상태

- https://15678910.github.io/lprc/ · https://github.com/15678910/lprc
- 상장사 1,883곳 · 업종 45개 벤치마크 · 공공기관 355곳 · 기능별 10개 그룹
- 첫 로드 134KB 그대로 — 공공부문 목록 66KB 는 ⑩ 카드에 스크롤이 가까워질 때만 받는다
  (⑩ 까지 안 내려가는 이용자는 아예 안 받는다). 그룹 상세는 기관을 고른 뒤에 받는다
- 매일 KST 10:00 자동 수집. 키 4개(DART·BOK·FRED·KOSIS) 모두 Actions Secrets 등록됨.
  알리오와 OECD(GNI·NNI 포함)는 키 불필요
- ①~⑪ 정상 · 미해결 데이터 없음

## 개발 메모

```bash
python -m http.server 8000 --directory docs   # 화면만 보기 (file:// 는 JSON 을 못 읽는다)
```

- 수집기는 **표준 라이브러리만** 쓴다. 의존성을 늘리지 말 것.
- `docs/*.json` 은 생성물이다. 손으로 고치면 다음 수집이 덮어쓴다
  (급하면 수집기와 생성물을 **함께** 고쳐야 한다).
- 화면 문구를 바꿀 때는 이 프로젝트의 태도를 지킬 것 — **과장하면 반박당한다.**
  한쪽에 불리한 한계도 화면에 적고, 부호가 뒤집히는 지표는 근거로 쓰지 않는다.
